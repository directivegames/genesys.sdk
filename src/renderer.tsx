import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Hello from Electron + React + TS + ESM!</h1>;

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
